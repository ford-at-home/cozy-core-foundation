A very short piece with no headings at all. Its blocks live in the implicit
zeroth section, so the anchors read from the lead-in counter rather than
stealing the first real section slot.

A second paragraph, to prove the paragraph counter advances inside the
zeroth section.
