// Mirror of supabase/functions/_shared/recognition.ts — change both
// together or neither (same convention as CREDIT_COST).

/** Confidence below this is highlighted as needing attention in review. */
export const LOW_CONFIDENCE_THRESHOLD = 0.7;
