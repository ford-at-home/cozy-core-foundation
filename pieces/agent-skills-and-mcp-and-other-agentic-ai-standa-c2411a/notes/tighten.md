# Tighten

- None open. The author's markup pass returned "Perfect" — the draft was
  approved without edits, so no new tightening questions were raised.

Notes from the finalization pass:

- `final.md` matches `draft.md` except the cover-image URL, which is re-pinned
  to the commit on this branch that carries the asset (immutable raw URL).
- Two `[Source needed: …]` markers remain in the published piece by design
  (Agent Skills adoption outside Anthropic/Claude; NLWeb-to-MCP default
  posture). Both are tracked in `to-research.md`; resolving them is the
  highest-leverage improvement for a v2.
- No new section illustrations were added beyond the existing cover: the
  "Perfect" annotation approved the printed layout as-is, and injecting
  visuals afterward would change a document the author already signed off on.
